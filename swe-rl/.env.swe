# SWE-RL environment config — source before launching training or eval.
# Usage: set -a; source .env.swe; set +a

WANDB_API_KEY=KEY
SWE_EXEC_SERVER_URLS="http://172.16.0.10:5000"
HF_CKPT=/path/to/model/snapshot
PROMPT_DATA=/path/to/train.jsonl
